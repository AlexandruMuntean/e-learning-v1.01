@model IEnumerable<E_LearningApplication.Models.ViewModels.ResourceViewModel>
<div class="form-panel">
    <h2><i class="fa fa-angle-right"></i> Display Module Resources</h2>
    <div class="panel panel-body panel-default">
        <table class="table table-striped table-hover">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FileName)
                </th>
                <th></th>
            </tr>
            @foreach (var item in Model) {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FileName)
                    </td>
                    <td>
                        @using (Html.BeginForm("DeleteModuleResource", "Prof", new { id = item.ResourceId }, FormMethod.Post)) {
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-danger btn-xs" title="Delete module resources">
                                <i class="fa fa-trash-o"></i> Delete
                            </button>
                        }
                        <button class="btn btn-info btn-xs" title="Replace module resources">
                            <i class="fa fa-refresh"></i>
                            @Html.ActionLink("Replace", "ReplaceModuleResource", new { id = item.ResourceId })
                        </button>
                    </td>
                </tr>
            }
        </table>
    </div>
    <div>
        <button type="button" class="btn btn-link mt">
            <i class="fa fa-arrow-left"></i>
            @Html.ActionLink("Back to Courses List", "DisplayCourses")
        </button>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('[data-toggle="tooltip"]').tooltip();
    });
</script>
